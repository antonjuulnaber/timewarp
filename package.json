{
	"devDependencies": {
		"fs-extra": "^9.0.1",
		"minify": "^6.0.0",
		"typescript": "^4.0.3"
	},
	"scripts": {
		"build": "tsc && node build-scripts/run-build.js",
		"build-dev": "tsc --watch"
	}
}
